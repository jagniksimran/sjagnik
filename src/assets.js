// Central place to map local image assets used across the site.
//
// Files live in src/assets/ and are imported so Vite fingerprints and bundles
// them. Add new resources here and reference them from components.

import heroImage from './assets/hero.jpg';

export const assets = {
  heroImage,
};
